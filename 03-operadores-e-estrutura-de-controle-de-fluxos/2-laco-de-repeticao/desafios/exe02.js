/**Iterando um Array com For…of: Use o laço for...of para iterar sobre um array de strings e imprimir cada string. */

const nome = ['Diemeson','Sofia','alemão'];
for(const pessoa of nome){
    console.log(pessoa);
}